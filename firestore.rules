rules_version = '2';
// Dev-only: allow all reads and writes. Never use in production.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
